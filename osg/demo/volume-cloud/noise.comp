#version 450

layout(r32f, binding = 0) uniform image3D noiseTex;
layout(local_size_x = 16, local_size_y = 16, local_size_z = 4) in;

void main()
{
	ivec3 pos = ivec3(gl_GlobalInvocationID.xyz);
	imageStore(noiseTex, pos, vec4(1000, 1000, 1000,1000));
}
